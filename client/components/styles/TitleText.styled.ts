import styled from 'styled-components'

export const STitleText = styled.h1`
  font-weight: 700;
  font-size: 28px;
  line-height: 35px;
  color: ${({theme}) => theme.colors.text};
  margin-top: 40px;
  margin-bottom: 20px;
`
